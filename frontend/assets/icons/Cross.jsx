import Entypo from '@expo/vector-icons/Entypo';

const Cross = ({ color = "#332c49", size = 24 }) => {
    return (
        <Entypo name="cross" size={size} color={color} />
    );
}

export default Cross;